<%= simple_form_for(@question_taken) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :question %>
    <%= f.association :user %>
    <%= f.input :attempts %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
