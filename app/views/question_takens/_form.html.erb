<%= form_for @question_taken, :html => { :class => 'form-horizontal form-model' } do |f| %>
  <div class="form-group">
    <%= f.label :attempts, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.number_field :attempts, :class => 'text-field form-control' %>
    </div>
  </div>

  <div class="form-group">
    <div class='col-md-offset-2 col-md-10'>
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                  question_takens_path, :class => 'btn btn-default' %>
    </div>
  </div>
<% end %>
