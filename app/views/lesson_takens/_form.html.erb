<%= simple_form_for(@lesson_taken) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :lesson %>
    <%= f.association :user %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
